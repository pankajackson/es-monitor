apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-ns.fullname" . }}
  labels:
    {{- include "grafana-ns.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-ns.selectorLabels" . | nindent 4 }}

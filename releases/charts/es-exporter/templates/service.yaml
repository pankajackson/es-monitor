apiVersion: v1
kind: Service
metadata:
  name: {{ include "es-exporter.fullname" . }}
  labels:
    {{- include "es-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "es-exporter.selectorLabels" . | nindent 4 }}
